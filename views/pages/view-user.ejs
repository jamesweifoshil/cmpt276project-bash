<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <% include ../partials/header.ejs %>
    <% include ../partials/nav-out.ejs %>
  </head>
  <body>
    <h1>Detail of user <%= rows.username %> </h1>
<   <form action="/delete-user/<%= rows.id %>" method="post">
      <div class="form-group row">
        <label class="col-form-label col-sm-1">Username</label>
        <div class="col-sm-2">
          <input autofocus readonly class="form-control" type="username" id="username" name="username" value="<%= rows.username %>"/>
        </div>
      </div>
      <div class="form-group row">
        <label class="col-form-label col-sm-1">Email</label>
        <div class="col-sm-2">
          <input autofocus readonly class="form-control" type="email" id="email" name="email" value="<%= rows.email %>"/>
        </div>
      </div>
      <div class="form-group row">
        <label class="col-form-label col-sm-1"></label>
        <div class="col-sm-10">
            <input type="submit" value="Delete this user" class="btn btn-sm btn-danger"/>
            <a class="btn btn-sm btn-success" href="/db">Cancel</a>
        </div>
      </div>
    </form>
  </body>
</html>
